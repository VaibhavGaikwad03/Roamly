# Optional: enable Google Places search.
# Without this, Roamly searches with OpenStreetMap (no key required).
#
# Create a browser (HTTP-referrer restricted) key with the
# "Places API (New)" enabled, then copy this file to `.env`:
VITE_GOOGLE_PLACES_KEY=
